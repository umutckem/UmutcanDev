@{
    ViewData["Title"] = "Gizlilik Politikası";
}

<div class="privacy-page">
    <div class="container">
        <!-- Header Section -->
        <div class="privacy-header" data-aos="fade-down">
            <div class="header-icon">
                <i class="bi bi-shield-check"></i>
            </div>
            <h1>Gizlilik Politikası</h1>
            <p>Kişisel verilerinizin korunması bizim için en yüksek önceliğe sahiptir</p>
        </div>

        <!-- Content Section -->
        <div class="privacy-content" data-aos="fade-up">
            <!-- Introduction -->
            <div class="content-section">
                <div class="section-header">
                    <i class="bi bi-info-circle"></i>
                    <h2>Genel Bakış</h2>
                </div>
                <p>UmutcanDev olarak, ziyaretçilerimizin ve kullanıcılarımızın gizliliğine büyük önem veriyoruz. Bu politika, kişisel verilerinizin nasıl toplandığını, kullanıldığını ve korunduğunu açıklamaktadır.</p>
            </div>

            <!-- Data Collection -->
            <div class="content-section" data-aos="fade-up">
                <div class="section-header">
                    <i class="bi bi-database"></i>
                    <h2>Veri Toplama ve Kullanım</h2>
                </div>
                <div class="feature-list">
                    <div class="feature-item">
                        <i class="bi bi-check-circle"></i>
                        <span>Sadece gerekli olan minimum kişisel veri toplanır</span>
                    </div>
                    <div class="feature-item">
                        <i class="bi bi-check-circle"></i>
                        <span>Toplanan veriler sadece belirtilen amaçlar için kullanılır</span>
                    </div>
                    <div class="feature-item">
                        <i class="bi bi-check-circle"></i>
                        <span>Verileriniz hiçbir şekilde üçüncü taraflarla paylaşılmaz</span>
                    </div>
                </div>
            </div>

            <!-- Data Security -->
            <div class="content-section" data-aos="fade-up">
                <div class="section-header">
                    <i class="bi bi-lock"></i>
                    <h2>Veri Güvenliği</h2>
                </div>
                <div class="feature-list">
                    <div class="feature-item">
                        <i class="bi bi-shield-check"></i>
                        <span>Verileriniz endüstri standartlarında şifreleme ile korunur</span>
                    </div>
                    <div class="feature-item">
                        <i class="bi bi-shield-check"></i>
                        <span>Düzenli güvenlik denetimleri ve güncellemeler yapılır</span>
                    </div>
                    <div class="feature-item">
                        <i class="bi bi-shield-check"></i>
                        <span>Erişim sadece yetkili personelle sınırlıdır</span>
                    </div>
                </div>
            </div>

            <!-- Cookies -->
            <div class="content-section" data-aos="fade-up">
                <div class="section-header">
                    <i class="bi bi-cookie"></i>
                    <h2>Çerezler (Cookies)</h2>
                </div>
                <p>Web sitemiz, kullanıcı deneyimini iyileştirmek için çerezler kullanmaktadır. Çerezler, tarayıcınızda saklanan küçük metin dosyalarıdır ve kişisel olarak tanımlanabilir bilgi içermezler.</p>
            </div>

            <!-- Contact -->
            <div class="contact-section" data-aos="fade-up">
                <div class="contact-header">
                    <i class="bi bi-envelope"></i>
                    <h3>Daha fazla bilgi için</h3>
                </div>
                <p>Gizlilik politikamızla ilgili herhangi bir sorunuz varsa, lütfen bizimle iletişime geçmekten çekinmeyin.</p>
                <a href="mailto:umutcankemahli637@gmail.com" class="contact-btn">
                    <i class="bi bi-envelope-fill"></i>
                    <span>umutcankemahli637@gmail.com</span>
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    :root {
        --primary-color: #1e3c72;
        --primary-light: #2a5298;
        --secondary-color: #4cc9f0;
        --dark-color: #1a1a2e;
        --light-color: #f8f9fa;
        --text-color: #333;
        --text-light: #6c757d;
        --border-radius: 12px;
        --box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        --transition: all 0.3s ease;
    }

    .privacy-page {
        min-height: 100vh;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 2rem 0;
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }

    .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 0 1.5rem;
    }

    /* Header Styles */
    .privacy-header {
        text-align: center;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 3rem 2rem;
        margin-bottom: 2rem;
        box-shadow: var(--box-shadow);
        border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .header-icon {
        width: 80px;
        height: 80px;
        margin: 0 auto 1.5rem;
        background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 2rem;
        box-shadow: 0 10px 25px rgba(30, 60, 114, 0.3);
    }

    .privacy-header h1 {
        font-size: 2.5rem;
        font-weight: 800;
        color: var(--dark-color);
        margin-bottom: 1rem;
        background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .privacy-header p {
        font-size: 1.1rem;
        color: var(--text-light);
        max-width: 600px;
        margin: 0 auto;
    }

    /* Content Styles */
    .privacy-content {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 2.5rem;
        box-shadow: var(--box-shadow);
        border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .content-section {
        margin-bottom: 2.5rem;
        padding: 1.5rem;
        background: rgba(255, 255, 255, 0.7);
        border-radius: 15px;
        border: 1px solid rgba(30, 60, 114, 0.1);
        transition: var(--transition);
    }

    .content-section:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .section-header {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1.5rem;
        padding-bottom: 0.75rem;
        border-bottom: 2px solid rgba(30, 60, 114, 0.1);
    }

    .section-header i {
        font-size: 1.5rem;
        color: var(--primary-color);
    }

    .section-header h2 {
        font-size: 1.4rem;
        font-weight: 700;
        color: var(--dark-color);
        margin: 0;
    }

    .content-section p {
        line-height: 1.8;
        color: var(--text-color);
        font-size: 1.05rem;
        margin: 0;
    }

    /* Feature List */
    .feature-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .feature-item {
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
        padding: 1rem;
        background: linear-gradient(135deg, rgba(76, 201, 240, 0.05), rgba(30, 60, 114, 0.05));
        border-radius: 10px;
        border-left: 3px solid var(--secondary-color);
        transition: var(--transition);
    }

    .feature-item:hover {
        background: linear-gradient(135deg, rgba(76, 201, 240, 0.1), rgba(30, 60, 114, 0.1));
        transform: translateX(5px);
    }

    .feature-item i {
        color: var(--secondary-color);
        font-size: 1.2rem;
        margin-top: 2px;
        flex-shrink: 0;
    }

    .feature-item span {
        color: var(--text-color);
        font-size: 1rem;
        line-height: 1.6;
    }

    /* Contact Section */
    .contact-section {
        background: linear-gradient(135deg, rgba(30, 60, 114, 0.1), rgba(76, 201, 240, 0.1));
        padding: 2rem;
        border-radius: 15px;
        text-align: center;
        border: 1px solid rgba(30, 60, 114, 0.2);
    }

    .contact-header {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        margin-bottom: 1rem;
    }

    .contact-header i {
        font-size: 1.5rem;
        color: var(--primary-color);
    }

    .contact-header h3 {
        font-size: 1.4rem;
        color: var(--dark-color);
        font-weight: 700;
        margin: 0;
    }

    .contact-section p {
        color: var(--text-light);
        font-size: 1.05rem;
        margin-bottom: 1.5rem;
    }

    .contact-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 1rem 2rem;
        background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
        color: white;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        transition: var(--transition);
        box-shadow: 0 8px 25px rgba(30, 60, 114, 0.3);
        font-size: 1.05rem;
    }

    .contact-btn:hover {
        background: linear-gradient(135deg, var(--primary-light), var(--primary-color));
        box-shadow: 0 12px 35px rgba(30, 60, 114, 0.4);
        transform: translateY(-3px);
        color: white;
    }

    .contact-btn i {
        font-size: 1.1rem;
    }

    /* Responsive Design */
    @@media (max-width: 768px) {
        .container {
            padding: 0 1rem;
        }

        .privacy-header {
            padding: 2rem 1.5rem;
        }

        .privacy-header h1 {
            font-size: 2rem;
        }

        .privacy-content {
            padding: 1.5rem;
        }

        .content-section {
            padding: 1rem;
        }

        .section-header h2 {
            font-size: 1.2rem;
        }

        .header-icon {
            width: 60px;
            height: 60px;
            font-size: 1.5rem;
        }
    }

    @@media (max-width: 576px) {
        .privacy-page {
            padding: 1rem 0;
        }

        .privacy-header h1 {
            font-size: 1.8rem;
        }

        .contact-btn {
            padding: 0.8rem 1.5rem;
            font-size: 1rem;
        }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Smooth animations for feature items
        const featureItems = document.querySelectorAll('.feature-item');
        featureItems.forEach(item => {
            item.addEventListener('mouseenter', function() {
                const icon = this.querySelector('i');
                if (icon) {
                    icon.style.transform = 'scale(1.2)';
                    icon.style.transition = 'transform 0.3s ease';
                }
            });

            item.addEventListener('mouseleave', function() {
                const icon = this.querySelector('i');
                if (icon) {
                    icon.style.transform = 'scale(1)';
                }
            });
        });

        // Contact button animation
        const contactBtn = document.querySelector('.contact-btn');
        if (contactBtn) {
            contactBtn.addEventListener('mouseenter', function() {
                const icon = this.querySelector('i');
                if (icon) {
                    icon.style.transform = 'translateX(3px)';
                    icon.style.transition = 'transform 0.3s ease';
                }
            });

            contactBtn.addEventListener('mouseleave', function() {
                const icon = this.querySelector('i');
                if (icon) {
                    icon.style.transform = 'translateX(0)';
                }
            });
        }

        // Header icon animation
        const headerIcon = document.querySelector('.header-icon');
        if (headerIcon) {
            headerIcon.addEventListener('mouseenter', function() {
                this.style.transform = 'scale(1.1) rotate(5deg)';
                this.style.transition = 'transform 0.3s ease';
            });

            headerIcon.addEventListener('mouseleave', function() {
                this.style.transform = 'scale(1) rotate(0deg)';
            });
        }
    });
</script>